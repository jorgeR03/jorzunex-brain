# infra/ — Infraestructura

Fase local (actual, coste 0€): `docker-compose.yml` con Postgres+pgvector (y más adelante n8n, Langfuse).
Fase VPS: configuración Coolify (Hetzner, ver ADR-0001 §2). No añadir servicios que el roadmap no pida.
